<html lang="en">
    <head>
        <meta charset="UTF-8">
        <link href="./static/styles.css" rel="stylesheet" type="text/css">
        <title>Product Types</title>
    </head>
    <body>
        <h1>Product Types</h1>
        <ul>
            <li><a href="/customers">Customers</a></li> 
            <li><a href="/orders">Orders</a></li> 
            <li><a href="/products">Products</a></li> 
            <li><a href="/scents">Scents</a></li> 
            <li><a href="/product_types">Product Types</a></li> 
        </ul>
        <table>
        <thead>
        <tr>
            <!-- Iterate through each key in the first entry to get the
            column name -->
            {% for key in data[0].keys() %}

            <!-- Create a <th> tag with the key inside of it, this will be
            our header row -->
            <th>{{ key }}</th>

            <!-- End of this for loop -->
            {% endfor %}
        </tr>
        <tbody>
        <p>{{ data }}</p>

        <!-- Now, iterate through every scent in Scents -->
        {% for product_type in data %}
        <tr>
            <!-- Then iterate through every key in the current product_type dictionary -->
            {% for key in product_type.keys() %}

            <!-- Create a <td> element with the value of that key in it -->
            <td>{{product_type[key]}}</td>
            {% endfor %}
            <td><a href="delete_product_type/{{product_type.product_type_id}}" class="btn btn-default">Delete</a></td>
            <td><a href="edit_product_type/{{product_type.product_type_id}}" class="btn btn-default">Edit</a></td>
        </tr>
        {% endfor %}
        </table>
        
        <!--BEGIN INSERT-->
        <div id="insert">
		<!-- form to INSERT new data into Product Types-->
		<form id="insertProductTypes" action="/product_types" method="post">
			<legend><strong>Add Product Type</strong></legend>
			<fieldset>
                <!-- placeholder shows ghost text, required forces an input, pattern forces input adherence -->
				<label for="model">Description:</label>
				<input type="text" class="form-control" id="description" name="description" maxlength="256" placeholder="" required>

            </fieldset>

            <!-- Create 'Add Product Type' and 'Cancel' buttons -->
			<input type="submit" value="Add Product Type" name="Add_Product_Type" class="btn btn-primary" style="margin:.5rem;">
			<input class="btn" type="button" value="Cancel" onClick="browsePeople()">
        </form>
	</div><!--insert-->
    </body>
</html>


